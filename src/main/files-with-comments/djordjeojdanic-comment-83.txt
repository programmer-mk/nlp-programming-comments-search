/// Sortiranje string vrednosti rastuce.

public IEnumerable<string> SortirajStringoveRastuce(IEnumerable<string> stringovi)
        {
            var stringoviRastuce = stringovi?.OrderBy(str => str);
            return stringoviRastuce;
        }